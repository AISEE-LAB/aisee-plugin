# Aisee Team Knowledge Template

This template is plugin content. Copy it from the installed Aisee marketplace plugin into a dedicated team knowledge repository when starting a shared knowledge base.

Persistent sources:

- `knowledge/cards/**/*.md`
- `knowledge/packs/*.yaml`

Caches and generated indexes are not facts and can be rebuilt.
