# aisee:srs — SRS Document Template

Read this file at the start of Phase 4 (SRS Generation). Choose the correct template section based on the output mode determined in Phase 3.

---

## Requirement Quality Checklist

Apply to **each FR** before writing it:

- [ ] Describes **what** the system does, not **how** (no implementation decisions)
- [ ] Testable — a QA engineer can write a test case from it
- [ ] No framework choices, API endpoint names, or DB schema details
- [ ] Priority is assigned (P0 / P1 / P2)
- [ ] Dependencies on other FRs are explicit
- [ ] **Scenario extension appended** — see Scenario Extension Blocks below

Fix silently if a requirement fails these checks. Do not stop to ask.

---

## FR 写作规则：基础块 + 场景扩展块

每条 FR 由两部分组成：

1. **基础块**（所有 FR 必须有）：描述、前置条件、主流程、异常流程、验收标准
2. **场景扩展块**（按场景类型追加）：根据 FR 所属场景，从下方"场景扩展块"章节中选取对应模板追加在基础块之后

**场景识别方式**：阅读 FR 描述，匹配下表中的识别信号，一个 FR 可能匹配多个场景类型。

| 场景类型 | 识别信号 |
|---|---|
| 表单/新建编辑页 | 关键词：新增、编辑、填写、提交、创建、修改 |
| 列表/查询页 | 关键词：查看列表、搜索、筛选、分页、导出 |
| 详情页 | 关键词：查看详情、详情页、信息展示 |
| 外部集成/API 对接 | 关键词：对接、集成、调用第三方、同步、推送 |
| 数据导入/导出 | 关键词：导入、导出、上传文件、下载、批量 |
| 通知/消息 | 关键词：通知、提醒、消息、邮件、短信、推送 |
| 权限/角色 | 关键词：权限、角色、访问控制、可见范围 |
| 工作流/审批 | 关键词：审批、流程、状态流转、待办、驳回 |

---

## 主文档模板（Epic 模式专用）

仅在 Epic 模式下使用。省略 Section 3 FR 详情，其余章节完整填写。

```markdown
# 软件需求规格说明书（主文档）

**文档编号**：SRS-{YYYY-MM-DD}-{slug}-main
**版本**：v1.0
**状态**：草稿
**创建日期**：{date}
**作者**：{从 AGENTS.md 或项目上下文提取，或填"待填写"}

---

## 模块文档索引

| 模块名称 | 文档路径 | FR 范围 | 优先级 |
|----------|---------|---------|--------|
| {模块 A} | [`./01-{module-a}.md`](./01-{module-a}.md) | FR-001 ~ FR-00X | P0 |
| {模块 B} | [`./02-{module-b}.md`](./02-{module-b}.md) | FR-00X ~ FR-00Y | P1 |

---

## 1. 引言

### 1.1 目的
本文档描述 {系统/功能名称} 的软件需求，供开发团队、产品负责人及相关干系人使用。
各功能模块的详细 FR 见上方"模块文档索引"中的对应文档。

### 1.2 范围
{两到三句话说明系统边界：它做什么、不做什么、与哪些系统交互}

### 1.3 术语与缩写
| 术语 | 定义 |
|------|------|
| {term} | {definition} |

### 1.4 参考文档
- {已引用文档，或填"暂无"}

---

## 2. 整体描述

### 2.1 产品概述
{系统定位、解决的核心问题、与现有系统的关系}

### 2.2 目标用户
| 用户角色 | 描述 | 主要诉求 |
|----------|------|----------|
| {role} | {desc} | {need} |

### 2.3 运行环境
{运行平台、依赖服务、技术栈约束（从 AGENTS.md / project.md 提取）}

### 2.4 设计约束
{技术选型约束、合规要求、现有架构限制}

### 2.5 现有基线影响

> 仅在 baseline-aware 模式下填写。只记录业务行为层面的影响，不写接口、数据库、代码实现或迁移方案。

| 类型 | 现有行为 / 规范 | 本需求影响 | 处理方式 | 来源 |
|---|---|---|---|---|
| 新增 / 修改 / 移除 / 兼容 | {existing behavior or 无} | {impact} | {新增 FR / 修改既有行为 / 移除待确认 / 保持兼容} | {openspec/specs/... or docs/... or 用户说明} |

---

## 3. 功能需求

> 本文档不包含 FR 详情，各模块 FR 见上方"模块文档索引"中的对应文档。

---

## 4. 非功能需求

### 4.1 性能
| 指标 | 要求 | 备注 |
|------|------|------|
| 响应时间 | {e.g., P99 < 500ms} | |
| 并发用户数 | {e.g., 1000} | |
| 列表查询 | {e.g., 10万条数据下 < 1s} | |

### 4.2 安全
- {认证/授权要求}
- {数据敏感性要求，如：手机号脱敏展示}

### 4.3 可靠性
- {可用性目标，e.g., 99.9%}
- {数据持久性要求}

### 4.4 可维护性
- {日志、监控、告警要求}

### 4.5 其他
- {国际化、无障碍访问、合规要求等}

---

## 5. 约束与假设

### 5.1 已确认约束
- {constraint 1}

### 5.2 假设（完整列表）
| 编号 | 假设内容 | 影响模块 | 影响需求 | 确认状态 |
|------|----------|----------|----------|----------|
| A-001 | {assumption} | {模块名} | {FR-xxx} | 待确认 |

---

## 6. 待确认事项（Open Questions）

| 编号 | 问题 | 影响范围 | 负责人 | 截止日期 |
|------|------|----------|--------|----------|
| Q-001 | {question} | {FR-xxx} | 待指定 | 待定 |

---

## 7. 变更候选清单（全局）

> 以下功能需求按优先级和依赖关系排列，可作为 `aisee:change-plan` 的输入。FR ID 链接到对应模块文档。

| 优先级 | 需求 ID | 标题 | 变更类型 | 影响基线 | 所属模块 | 估计规模 | 依赖 |
|--------|---------|------|----------|----------|----------|----------|------|
| P0 | [FR-001](./01-{module-a}.md#fr-001) | {title} | 新增 / 修改 / 移除 / 兼容 | {baseline ref or 无} | {模块 A} | M | 无 |
| P0 | [FR-002](./01-{module-a}.md#fr-002) | {title} | 修改 | {openspec/specs/...} | {模块 A} | S | FR-001 |
| P1 | [FR-003](./02-{module-b}.md#fr-003) | {title} | 新增 | 无 | {模块 B} | L | 无 |

**下一步**：将各模块文档路径传入 `aisee:change-plan`，进行 Change 边界规划：

\`\`\`
aisee:change-plan docs/requirements/{slug}/01-{module-a}.md
aisee:change-plan docs/requirements/{slug}/02-{module-b}.md
\`\`\`
```

---

## 模块文档模板（Epic 模式专用）

每个功能模块单独生成一份文档，使用此模板。

```markdown
# {模块名称} — 需求详情文档

**所属主文档**：[`./00-main.md`](./00-main.md)
**文档编号**：SRS-{YYYY-MM-DD}-{slug}-{module-slug}
**版本**：v1.0
**状态**：草稿
**创建日期**：{date}
**FR 范围**：FR-{XXX} ~ FR-{YYY}

---

## 模块概述

{1–2 句话说明本模块的职责范围，以及与其他模块的关系。}

---

## 3. 功能需求

> 每条需求使用唯一 ID（FR-001、FR-002…），与主文档 Section 7 对应。

### 3.{N} {功能模块名称}

#### FR-{XXX} {需求标题}

**描述**：{一句话说明该需求交付什么}

**前置条件**：{触发该功能前系统或用户需满足的条件}

**主流程**：
1. {step 1}
2. {step 2}
3. {step 3}

**异常流程**：
- {exception 1} → {system response}
- {exception 2} → {system response}

**验收标准**：
- [ ] {criterion 1}
- [ ] {criterion 2}

**优先级**：P0 / P1 / P2
**变更类型**：新增 / 修改 / 移除 / 兼容
**影响基线**：{openspec/specs/... or docs/... or "无"}
**依赖**：{FR-xxx 或 "无"}

{↓ 根据场景类型追加对应扩展块，无匹配场景则删除此行}

---

## 4. 本模块非功能补充

> 只记录本模块特有的性能、安全、可靠性、可维护性要求；全局非功能要求见主文档 Section 4。无模块级补充时填"无"。

- {requirement or "无"}

---

## 5. 本模块相关假设

> 以下假设已在主文档 Section 5.2 完整记录，此处仅列编号和摘要供快速参考。

| 编号 | 假设摘要 | 确认状态 |
|------|----------|----------|
| A-001 | {brief summary} | 待确认 |

---

## 6. 本模块待确认事项（Open Questions）

| 编号 | 问题 | 影响需求 | 负责人 | 截止日期 |
|------|------|----------|--------|----------|
| Q-001 | {question} | {FR-xxx} | 待指定 | 待定 |

---

## 7. 模块变更候选清单

> 以下功能需求按优先级和依赖关系排列，可作为 `aisee:change-plan` 的输入。只列出本模块范围内的 FR。

| 优先级 | 需求 ID | 标题 | 变更类型 | 影响基线 | 估计规模 | 依赖 |
|--------|---------|------|----------|----------|----------|------|
| P0 | FR-{XXX} | {title} | 新增 / 修改 / 移除 / 兼容 | {baseline ref or 无} | M | 无 |
| P1 | FR-{YYY} | {title} | 修改 | {openspec/specs/...} | S | FR-{XXX} |

**下一步**：将本文档路径传入 `aisee:change-plan`，进行 Change 边界规划：

\`\`\`
aisee:change-plan docs/requirements/{slug}/{module-file}.md
\`\`\`
```

---

## 标准文档模板（标准模式专用）

在 FR ≤ 10 且模块 < 3 时使用此模板，生成单份完整文档。

```markdown
# 软件需求规格说明书

**文档编号**：SRS-{YYYY-MM-DD}-{slug}
**版本**：v1.0
**状态**：草稿
**创建日期**：{date}
**作者**：{从 AGENTS.md 或项目上下文提取，或填"待填写"}

---

## 1. 引言

### 1.1 目的
本文档描述 {系统/功能名称} 的软件需求，供开发团队、产品负责人及相关干系人使用。

### 1.2 范围
{两到三句话说明系统边界：它做什么、不做什么、与哪些系统交互}

### 1.3 术语与缩写
| 术语 | 定义 |
|------|------|
| {term} | {definition} |

### 1.4 参考文档
- {已引用文档，或填"暂无"}

---

## 2. 整体描述

### 2.1 产品概述
{系统定位、解决的核心问题、与现有系统的关系}

### 2.2 目标用户
| 用户角色 | 描述 | 主要诉求 |
|----------|------|----------|
| {role} | {desc} | {need} |

### 2.3 运行环境
{运行平台、依赖服务、技术栈约束（从 AGENTS.md / project.md 提取）}

### 2.4 设计约束
{技术选型约束、合规要求、现有架构限制}

### 2.5 现有基线影响

> 仅在 baseline-aware 模式下填写。只记录业务行为层面的影响，不写接口、数据库、代码实现或迁移方案。

| 类型 | 现有行为 / 规范 | 本需求影响 | 处理方式 | 来源 |
|---|---|---|---|---|
| 新增 / 修改 / 移除 / 兼容 | {existing behavior or 无} | {impact} | {新增 FR / 修改既有行为 / 移除待确认 / 保持兼容} | {openspec/specs/... or docs/... or 用户说明} |

---

## 3. 功能需求

> 每条需求使用唯一 ID（FR-001、FR-002…），便于 `aisee:change-plan` 引用和追踪。

### 3.1 {功能模块名称}

#### FR-001 {需求标题}

**描述**：{一句话说明该需求交付什么}

**前置条件**：{触发该功能前系统或用户需满足的条件}

**主流程**：
1. {step 1}
2. {step 2}
3. {step 3}

**异常流程**：
- {exception 1} → {system response}
- {exception 2} → {system response}

**验收标准**：
- [ ] {criterion 1}
- [ ] {criterion 2}

**优先级**：P0 / P1 / P2
**变更类型**：新增 / 修改 / 移除 / 兼容
**影响基线**：{openspec/specs/... or docs/... or "无"}
**依赖**：{FR-xxx 或 "无"}

{↓ 根据场景类型追加对应扩展块，无匹配场景则删除此行}

---

## 4. 非功能需求

### 4.1 性能
| 指标 | 要求 | 备注 |
|------|------|------|
| 响应时间 | {e.g., P99 < 500ms} | |
| 并发用户数 | {e.g., 1000} | |
| 列表查询 | {e.g., 10万条数据下 < 1s} | |

### 4.2 安全
- {认证/授权要求}
- {数据敏感性要求，如：手机号脱敏展示}

### 4.3 可靠性
- {可用性目标，e.g., 99.9%}
- {数据持久性要求}

### 4.4 可维护性
- {日志、监控、告警要求}

### 4.5 其他
- {国际化、无障碍访问、合规要求等}

---

## 5. 约束与假设

### 5.1 已确认约束
- {constraint 1}

### 5.2 假设
| 编号 | 假设内容 | 影响需求 | 确认状态 |
|------|----------|----------|----------|
| A-001 | {assumption} | {FR-xxx} | 待确认 |

---

## 6. 待确认事项（Open Questions）

| 编号 | 问题 | 影响范围 | 负责人 | 截止日期 |
|------|------|----------|--------|----------|
| Q-001 | {question} | {FR-xxx} | 待指定 | 待定 |

---

## 7. 变更候选清单

> 以下功能需求按优先级和依赖关系排列，可作为 `aisee:change-plan` 的输入。

| 优先级 | 需求 ID | 标题 | 变更类型 | 影响基线 | 估计规模 | 依赖 |
|--------|---------|------|----------|----------|----------|------|
| P0 | FR-001 | {title} | 新增 / 修改 / 移除 / 兼容 | {baseline ref or 无} | M | 无 |
| P0 | FR-002 | {title} | 修改 | {openspec/specs/...} | S | FR-001 |
| P1 | FR-003 | {title} | 新增 | 无 | L | 无 |

**下一步**：将本文档路径传入 `aisee:change-plan`，进行 Change 边界规划：

\`\`\`
aisee:change-plan docs/requirements/{filename}.md
\`\`\`
```

---

## 场景扩展块

以下为各场景类型的扩展模板。在基础块末尾、`---` 分隔线之前追加匹配的扩展块。

---

### 扩展块 A：表单 / 新建 / 编辑页

适用场景：新增记录、编辑已有数据、包含填写和提交动作的页面。

```markdown
**页面字段规格**：

| 字段名 | 字段类型 | 必填 | 默认值 | 校验规则 | 备注 |
|--------|---------|------|--------|---------|------|
| {字段名} | 文本 / 数字 / 日期 / 下拉 / 多选 / 文件 / 富文本 | 是/否 | {默认值或"无"} | {规则，如：≤50字符、仅数字、日期不早于今天} | {联动关系或其他说明} |

**字段联动规则**：
- {字段 A} 选择 {值} 时，{字段 B} 显示/隐藏/变为必填
- {其他联动，无则删除此节}

**提交行为**：
- 提交成功：{跳转到列表页 / 留在当前页并清空 / 其他}
- 提交失败：{内联错误提示 / 顶部 Toast / 其他}
- 草稿/暂存：{支持 / 不支持}
```

---

### 扩展块 B：列表 / 查询页

适用场景：展示数据列表，支持搜索、筛选、排序、分页。

```markdown
**列表列定义**：

| 列名 | 数据来源 | 是否可排序 | 备注 |
|------|---------|-----------|------|
| {列名} | {字段} | 是/否 | {格式说明，如：日期格式 YYYY-MM-DD} |

**搜索与筛选条件**：

| 条件名 | 控件类型 | 匹配方式 | 默认值 |
|--------|---------|---------|--------|
| {条件名} | 输入框 / 下拉 / 日期范围 / 多选 | 模糊/精确/范围 | {默认值或"无"} |

**分页规格**：
- 默认每页条数：{N} 条，可选：{10 / 20 / 50 / 100}
- 超大数据量（>{阈值}条）时的处理：{懒加载 / 虚拟滚动 / 提示导出}

**行操作**：
- 单行操作：{查看详情 / 编辑 / 删除 / 其他}
- 批量操作：{批量删除 / 批量导出 / 批量状态变更 / 无}
- 操作权限控制：{按角色 / 按数据所有权 / 无限制}

**空状态**：
- 无数据时展示：{空态图 + 提示文案 / 仅提示文案}
- 搜索无结果时展示：{提示文案}
```

---

### 扩展块 C：详情页

适用场景：展示单条记录的完整信息，只读或部分可操作。

```markdown
**展示字段**：

| 字段名 | 展示格式 | 备注 |
|--------|---------|------|
| {字段名} | {原始值 / 映射值 / 格式化，如：状态码→中文标签} | {如：超长文本折叠} |

**可操作项**：
- {编辑 / 删除 / 状态变更 / 其他操作}：{触发条件，如：仅草稿状态可编辑}

**关联数据展示**：
- {关联模块名}：{展示方式，如：底部列表 / 侧边栏 / Tab 页签}
```

---

### 扩展块 D：外部集成 / API 对接

适用场景：调用第三方服务、与外部系统数据同步、Webhook 接收。

```markdown
**集成方式**：{主动调用 / Webhook 接收 / 双向同步}

**触发时机**：{用户操作触发 / 定时任务 / 事件触发，说明具体条件}

**数据映射**：

| 本系统字段 | 对方字段 | 转换规则 |
|-----------|---------|---------|
| {本系统字段} | {对方字段} | {直接映射 / 格式转换 / 枚举映射} |

**异常处理**：
- 对方服务超时（>{N}s）：{重试策略，如：退避重试 3 次 / 降级处理}
- 对方返回错误码：{各错误码对应的处理方式}
- 数据冲突（双向同步时）：{以本系统为准 / 以对方为准 / 人工介入}

**幂等性**：{需要 / 不需要，如需要说明幂等键设计思路}
```

---

### 扩展块 E：数据导入 / 导出

适用场景：批量上传文件、下载数据报表、Excel/CSV 处理。

```markdown
**文件规格**：
- 支持格式：{xlsx / csv / json / 其他}
- 最大文件大小：{N} MB
- 最大行数：{N} 行

**导入字段映射**：

| 列名（文件中） | 对应系统字段 | 必填 | 格式要求 | 错误处理 |
|--------------|------------|------|---------|---------|
| {列名} | {系统字段} | 是/否 | {如：日期格式 YYYY-MM-DD} | {跳过该行 / 终止导入 / 记录错误} |

**导入行为**：
- 重复数据处理：{覆盖 / 跳过 / 报错}
- 部分失败时：{全量回滚 / 成功行提交失败行报告}
- 导入结果反馈：{实时进度条 / 完成后通知 / 下载错误报告}

**导出行为**：
- 导出范围：{当前页 / 全部数据 / 勾选数据}
- 超大数据量（>{阈值}行）：{异步生成后通知下载 / 分批导出}
- 敏感字段：{脱敏导出 / 按权限控制是否包含}
```

---

### 扩展块 F：通知 / 消息

适用场景：站内消息、邮件、短信、App Push、Webhook 通知。

```markdown
**通知渠道**：{站内消息 / 邮件 / 短信 / App Push / 企业微信 / 钉钉 / Webhook}

**触发条件**：

| 触发事件 | 接收对象 | 渠道 | 是否可配置关闭 |
|---------|---------|------|--------------|
| {事件，如：审批通过} | {角色/具体用户} | {渠道} | 是/否 |

**消息内容模板**：

| 字段 | 内容 |
|------|------|
| 标题 | {模板，如：【{系统名}】您的申请已{状态}} |
| 正文 | {模板，变量用 {变量名} 表示} |
| 跳转链接 | {跳转到详情页 / 无} |

**频率控制**：
- 同一事件对同一用户：{不限 / 每日最多 N 次 / 合并推送}
- 免打扰时段：{有，{时段} / 无}
```

---

### 扩展块 G：权限 / 角色控制

适用场景：不同角色对功能或数据有不同的访问权限。

```markdown
**角色定义**：

| 角色名 | 描述 | 数据可见范围 |
|--------|------|------------|
| {角色} | {职责描述} | {全部 / 本部门 / 本人创建} |

**功能权限矩阵**：

| 功能点 | {角色 A} | {角色 B} | {角色 C} |
|--------|---------|---------|---------|
| 查看列表 | ✅ | ✅ | ✅ |
| 新增 | ✅ | ✅ | ❌ |
| 编辑 | ✅ | 仅本人 | ❌ |
| 删除 | ✅ | ❌ | ❌ |
| 导出 | ✅ | ❌ | ❌ |
| {其他功能点} | | | |

**无权限时的处理**：
- 功能入口：{隐藏按钮 / 置灰按钮 + Tooltip}
- 直接访问受限页面：{跳转登录 / 跳转 403 页 / 显示无权限提示}
```

---

### 扩展块 H：工作流 / 审批流程

适用场景：多步骤审批、状态机驱动的业务流程、待办任务。

```markdown
**状态定义**：

| 状态值 | 显示名称 | 说明 |
|--------|---------|------|
| {状态值} | {中文名} | {进入该状态的条件} |

**状态流转图**：

{状态 A} --[操作名称]--> {状态 B}
{状态 A} --[操作名称]--> {状态 C}
{状态 B} --[操作名称]--> {状态 D}

**各状态可执行操作**：

| 当前状态 | 可执行操作 | 执行角色 | 执行后状态 | 需要填写原因 |
|---------|---------|---------|----------|------------|
| {状态} | {操作名} | {角色} | {目标状态} | 是/否 |

**超时处理**：
- 在 {状态} 停留超过 {N} 天：{自动流转 / 发送催办通知 / 无}

**审批记录**：
- 是否保留完整流转历史：{是 / 否}
- 历史记录展示位置：{详情页时间轴 / 独立记录 Tab / 不展示}
```

---

## Section Writing Notes

**Section 3 — 场景扩展块使用原则**

- 每条 FR 至少检查一次场景匹配，确保不遗漏扩展块
- 一条 FR 可以追加多个扩展块（如：带权限控制的列表页 = 扩展块 B + 扩展块 G）
- 字段规格表中不允许出现"待定"——如果信息未收集到，放入 Section 6（Open Questions），注明影响的 FR
- 扩展块中的枚举选项（如字段类型、控件类型）应使用实际值，不保留括号内的选项列表
- 扩展块只记录功能性 UI 合约：字段、列、筛选、操作、状态、校验、权限、反馈。不要写视觉布局、组件库、颜色、图标或具体排版。
- 如果需要进一步说明页面清单、页面内容、页面元素和跨页面交互流程，在 SRS 之后生成独立的 UI 内容规格，不要塞进 FR 正文。

**Section 5.2（假设）**

- 每个对话中记录的 `[ASSUMPTION]` 必须出现在主文档或标准文档的这里
- Epic 模式下，模块文档中的假设仅作引用，完整内容以主文档为准
- 不因"显而易见"而省略假设条目

**Section 2.5（现有基线影响）**

- 仅在 baseline-aware 模式下填写；没有可识别基线时写“未发现可信基线来源”，不要猜。
- 只记录用户可观察业务行为、角色权限、状态、流程、入口、兼容要求的影响。
- 不写 API endpoint、数据库表字段、ORM、代码模块、迁移脚本或实现方案。
- 每条受影响 FR 必须在 FR 正文和 Section 7 中标注“变更类型”和“影响基线”。
- 影响基线来源优先级：`openspec/specs/` > active change artifacts > `docs/spec-migration/` > 历史 SRS > 用户明确说明 > 从代码推断（低可信，必须标注）。

**Section 6（Open Questions）**

- 对话中提及但未解决的事项放这里，不放进 FR 正文
- 用户未提及但实现时必须明确的事项也放这里，注明：`(未在需求中提及 — 实施前请确认)`

**Section 7（变更候选清单）**

- 规模估算是粗估，aisee:change-plan 会细化
- 依赖关系与 FR 正文中的"依赖"字段保持一致
- Epic 模式下，主文档 Section 7 中的 FR ID 必须附相对路径链接指向对应模块文档
- baseline-aware 模式下，必须包含“变更类型”和“影响基线”列，便于 `aisee:change-plan` 生成 source-map seed
