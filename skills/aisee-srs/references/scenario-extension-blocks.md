# aisee:srs — Scenario Extension Blocks

Read only the blocks that match the FRs being written. These blocks keep SRS planning-level detailed; do not turn them into API, DB, component, or implementation design.

## 扩展块 A：表单 / 新建 / 编辑页

适用场景：新增记录、编辑已有数据、包含填写和提交动作的页面。

```markdown
**业务信息采集要求**：

| 信息项 | 信息类型 | 是否必需 | 业务校验规则 | 备注 |
|--------|----------|----------|--------------|------|
| {信息项} | 文本 / 数字 / 日期 / 选项 / 文件 / 其他 | 是/否 | {规则，如：≤50字符、仅数字、日期不早于今天} | {业务含义或说明} |

**业务联动规则**：
- 当 {信息项 A} 为 {值} 时，{信息项 B} 变为必需 / 不适用 / 只读
- {其他联动，无则删除此节}

**提交行为**：
- 提交成功：{跳转到列表页 / 留在当前页并清空 / 其他}
- 提交失败：{内联错误提示 / 顶部 Toast / 其他}
- 草稿/暂存：{支持 / 不支持}
```

## 扩展块 B：列表 / 查询页

适用场景：展示数据列表，支持搜索、筛选、排序、分页。

```markdown
**列表列定义**：

| 列名 | 数据来源 | 是否可排序 | 备注 |
|------|---------|-----------|------|
| {列名} | {字段} | 是/否 | {格式说明，如：日期格式 YYYY-MM-DD} |

**搜索与筛选条件**：

| 条件名 | 控件类型 | 匹配方式 | 默认值 |
|--------|---------|---------|--------|
| {条件名} | 输入框 / 下拉 / 日期范围 / 多选 | 模糊/精确/范围 | {默认值或"无"} |

**分页规格**：
- 默认每页条数：{N} 条，可选：{10 / 20 / 50 / 100}
- 超大数据量（>{阈值}条）时的处理：{懒加载 / 虚拟滚动 / 提示导出}

**行操作**：
- 单行操作：{查看详情 / 编辑 / 删除 / 其他}
- 批量操作：{批量删除 / 批量导出 / 批量状态变更 / 无}
- 操作权限控制：{按角色 / 按数据所有权 / 无限制}

**空状态**：
- 无数据时展示：{空态图 + 提示文案 / 仅提示文案}
- 搜索无结果时展示：{提示文案}
```

## 扩展块 C：详情页

适用场景：展示单条记录的完整信息，只读或部分可操作。

```markdown
**展示字段**：

| 字段名 | 展示格式 | 备注 |
|--------|---------|------|
| {字段名} | {原始值 / 映射值 / 格式化，如：状态码→中文标签} | {如：超长文本折叠} |

**可操作项**：
- {编辑 / 删除 / 状态变更 / 其他操作}：{触发条件，如：仅草稿状态可编辑}

**关联数据展示**：
- {关联模块名}：{展示方式，如：底部列表 / 侧边栏 / Tab 页签}
```

## 扩展块 D：外部系统协作

适用场景：与第三方服务、外部系统、Webhook、外部审批或数据同步存在业务协作。

```markdown
**协作方式**：{主动请求 / 被动接收 / 双向同步 / 人工导入导出 / 其他}

**触发时机**：{用户操作触发 / 定时任务 / 事件触发，说明具体条件}

**业务信息映射**：

| 本系统业务信息 | 对方业务信息 | 业务含义 / 转换说明 |
|-----------|---------|---------|
| {本系统信息} | {对方信息} | {直接对应 / 枚举映射 / 人工确认 / 待确认} |

**业务异常处理**：
- 外部系统不可用：{提示用户稍后重试 / 进入待处理 / 人工介入 / 其他}
- 外部系统拒绝或返回失败：{用户可见反馈和业务状态}
- 数据冲突（双向同步时）：{以本系统为准 / 以对方为准 / 人工介入 / 待确认}

**重复提交 / 重复回调处理**：{允许 / 拒绝 / 合并 / 待确认}
```

## 扩展块 E：数据导入 / 导出

适用场景：批量上传文件、下载数据报表、Excel/CSV 处理。

```markdown
**文件规格**：
- 支持格式：{xlsx / csv / json / 其他}
- 最大文件大小：{N} MB
- 最大行数：{N} 行

**导入字段映射**：

| 列名（文件中） | 对应系统字段 | 必填 | 格式要求 | 错误处理 |
|--------------|------------|------|---------|---------|
| {列名} | {系统字段} | 是/否 | {如：日期格式 YYYY-MM-DD} | {跳过该行 / 终止导入 / 记录错误} |

**导入行为**：
- 重复数据处理：{覆盖 / 跳过 / 报错}
- 部分失败时：{全量回滚 / 成功行提交失败行报告}
- 导入结果反馈：{实时进度条 / 完成后通知 / 下载错误报告}

**导出行为**：
- 导出范围：{当前页 / 全部数据 / 勾选数据}
- 超大数据量（>{阈值}行）：{异步生成后通知下载 / 分批导出}
- 敏感字段：{脱敏导出 / 按权限控制是否包含}
```

## 扩展块 F：通知 / 消息

适用场景：站内消息、邮件、短信、App Push、Webhook 通知。

```markdown
**通知渠道**：{站内消息 / 邮件 / 短信 / App Push / 企业微信 / 钉钉 / Webhook}

**触发条件**：

| 触发事件 | 接收对象 | 渠道 | 是否可配置关闭 |
|---------|---------|------|--------------|
| {事件，如：审批通过} | {角色/具体用户} | {渠道} | 是/否 |

**消息内容模板**：

| 字段 | 内容 |
|------|------|
| 标题 | {模板，如：【{系统名}】您的申请已{状态}} |
| 正文 | {模板，变量用 {变量名} 表示} |
| 跳转链接 | {跳转到详情页 / 无} |

**频率控制**：
- 同一事件对同一用户：{不限 / 每日最多 N 次 / 合并推送}
- 免打扰时段：{有，{时段} / 无}
```

## 扩展块 G：权限 / 角色控制

适用场景：不同角色对功能或数据有不同的访问权限。

```markdown
**角色定义**：

| 角色名 | 描述 | 数据可见范围 |
|--------|------|------------|
| {角色} | {职责描述} | {全部 / 本部门 / 本人创建} |

**功能权限矩阵**：

| 功能点 | {角色 A} | {角色 B} | {角色 C} |
|--------|---------|---------|---------|
| 查看列表 | yes | yes | yes |
| 新增 | yes | yes | no |
| 编辑 | yes | 仅本人 | no |
| 删除 | yes | no | no |
| 导出 | yes | no | no |
| {其他功能点} | | | |

**无权限时的处理**：
- 功能入口：{隐藏按钮 / 置灰按钮 + Tooltip}
- 直接访问受限页面：{跳转登录 / 跳转 403 页 / 显示无权限提示}
```

## 扩展块 H：工作流 / 审批流程

适用场景：多步骤审批、状态机驱动的业务流程、待办任务。

```markdown
**状态定义**：

| 状态值 | 显示名称 | 说明 |
|--------|---------|------|
| {状态值} | {中文名} | {进入该状态的条件} |

**状态流转图**：

{状态 A} --[操作名称]--> {状态 B}
{状态 A} --[操作名称]--> {状态 C}
{状态 B} --[操作名称]--> {状态 D}

**各状态可执行操作**：

| 当前状态 | 可执行操作 | 执行角色 | 执行后状态 | 需要填写原因 |
|---------|---------|---------|----------|------------|
| {状态} | {操作名} | {角色} | {目标状态} | 是/否 |

**超时处理**：
- 在 {状态} 停留超过 {N} 天：{自动流转 / 发送催办通知 / 无}

**审批记录**：
- 是否保留完整流转历史：{是 / 否}
- 历史记录展示位置：{详情页时间轴 / 独立记录 Tab / 不展示}
```
