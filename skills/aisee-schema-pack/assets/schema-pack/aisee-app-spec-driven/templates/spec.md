## ADDED Requirements

### Requirement: {{scope}}:SPEC-001 {{requirement-name}}

> SPEC ID 必须来自 `.aisee/id-registry.json`。工具不可用时使用 `{{scope}}:SPEC-NEW-001` 并在 `source-map.md` 标注 `[ID-RESERVATION-REQUIRED]`。

系统必须……

#### Scenario: {{scenario-name}}

- **GIVEN** 
- **WHEN** 
- **THEN** 

#### Acceptance Notes

- 覆盖上游 ID：
- 关联页面 / 流程 / 状态：
- 关联架构约束：
- 关联接口 / 服务能力：
- 关联数据：
- 关联测试：

## MODIFIED Requirements

### Requirement: {{scope}}:SPEC-xxx {{existing-requirement-name}}

<!-- 如修改既有行为，在此说明修改后的完整要求。无则删除本节。 -->

## REMOVED Requirements

### Requirement: {{scope}}:SPEC-xxx {{removed-requirement-name}}

<!-- 如移除既有行为，在此说明移除原因和兼容影响。无则删除本节。 -->
