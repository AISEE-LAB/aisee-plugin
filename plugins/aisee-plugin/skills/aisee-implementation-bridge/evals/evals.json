{
  "schema_version": "aisee.skill-eval.v1",
  "skill_name": "aisee:implementation-bridge",
  "harness": {
    "type": "skill-eval",
    "owner": "aisee",
    "fact_source_policy": "eval cases are test inputs, not runtime facts"
  },
  "evals": [
    {
      "id": "implementation-bridge-001",
      "name": "handoff-authored-change-to-ce-work",
      "scenario": "implementation-handoff",
      "prompt": "change 已 authored。帮我判断能否进入 ce-work，并只给出最小 JSON 判定结果：该先读什么、完成后要回写什么。",
      "context": {
        "files": [],
        "assumptions": [
          "当前 change 已存在 context pack。"
        ]
      },
      "expected_output": "应默认直接读取当前 change artifacts，并输出一次性的 JSON 读取策略与完成提醒；明确 read first、apply tracks、verification / evidence 入口和 review recommendation；不得创建平行长期任务清单、默认展开 Brief 正文或直接写代码。",
      "expected_outputs": [
        "先读当前 change 的事实源。",
        "输出 JSON 判定关键字段。",
        "明确完成后先回写 tasks.md 或 apply tracks。"
      ],
      "must_ask": [],
      "must_include": [
        "read first",
        "apply tracks",
        "verification",
        "review recommendation"
      ],
      "must_not": [
        "不得创建平行长期任务清单。",
        "不得默认输出完整 Implementation Brief。",
        "不得替代 ce-work、ce-code-review 或 aisee:verify。"
      ],
      "quality_checks": [
        "默认输出应保持为 JSON 判定，不展开整段人读摘要。",
        "scope 和写回位置必须来自当前 change。"
      ],
      "fact_source_constraints": [
        "Implementation Brief 不是长期规范事实源，正式结论必须回写当前 change artifacts 或 apply tracks。"
      ],
      "files": []
    }
  ]
}
