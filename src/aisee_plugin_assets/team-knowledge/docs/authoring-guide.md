# Authoring Guide

Write cards as short, reviewed guardrails.

Each card needs:

- `id`
- `title`
- `status`
- `applies_to`
- `trigger`
- `recommended_action`
- `boundaries`

Use `candidate` for drafts and `active` only after review. Prefer observable triggers and concrete actions. Boundaries should say when the card must not be applied.

