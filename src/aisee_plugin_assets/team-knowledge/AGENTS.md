# AGENTS.md

- Team knowledge cards are guardrails, not specs or task lists.
- Keep `knowledge/cards/**` and `knowledge/packs/**` as the only durable parse sources.
- Treat `indexes/**` as removable cache.
- Never include secrets, customer data, production URLs, private certificates, or copied solution bodies.
- Default new cards to `status: candidate`; switch to `active` only after human review.

